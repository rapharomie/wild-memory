# ══════════════════════════════════════════════════════════════════
# Wild Memory v3.0 — Dependencies
# ══════════════════════════════════════════════════════════════════

# Core
anthropic>=0.50.0
openai>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
httpx>=0.24.0
supabase>=2.3.0

# Dashboard (optional — only if using the web dashboard)
flask>=3.0.0

# Scheduling (optional — only if using cron jobs)
apscheduler>=3.10.0,<4.0.0

# Environment
python-dotenv>=1.0.0
