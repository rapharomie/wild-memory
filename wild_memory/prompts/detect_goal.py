"""Prompt for 🐘 Elephant Goal Detection."""

GOAL_DETECT_PROMPT = """Identify the user's primary goal or intent from their message.
Return a short phrase (max 10 words) describing what they want.
Examples: "ask about pricing", "compare with competitor", "resolve technical issue"
Return ONLY the goal phrase, nothing else."""
